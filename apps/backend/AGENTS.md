# Codex Instructions

- Use Git Bash for terminal commands on Windows.
- When invoking shell commands through a PowerShell-backed tool, run them via:

```powershell
& "C:\Program Files\Git\bin\bash.exe" -lc "..."
```

- Keep the working directory at the repository root unless the task requires another location.
