'use client'

import { BorrowListSection } from "@/components/borrow/borrow-list-section";

export default function BorrowPage() {
    return (
        <div className="h-full">
            <BorrowListSection />
        </div>
    );
}